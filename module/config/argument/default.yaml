# --------------------
# Define default values
# --------------------

# ==================== Alas ====================

# ==================== Farm ====================

GemsFarming:
  EnemyPriority:
    EnemyScaleBalanceWeight: S1_enemy_first
  Submarine:
    # 原 override.yaml 强制 use_open_ocean_support，放开潜艇限制后以任务默认保留原行为
    DistanceToBoss: use_open_ocean_support
ThreeOilLowCost:
  EnemyPriority:
    EnemyScaleBalanceWeight: S1_enemy_first
  Submarine:
    DistanceToBoss: use_open_ocean_support

# ==================== Event ====================

EventA:
  EventDaily:
    StageFilter: |-
      A1 > A2 > A3
EventB:
  EventDaily:
    StageFilter: |-
      B1 > B2 > B3
EventC:
  EventDaily:
    StageFilter: |-
      C1 > C2 > C3
EventD:
  EventDaily:
    StageFilter: |-
      D1 > D2 > D3

# ==================== Reward ====================

# ==================== DailyReward ====================

# ==================== DailyMission ====================

# ==================== Opsi ====================
OpsiCrossMonth:
  Scheduler:
    Sensitive: true
OpsiObscure:
  Scheduler:
    Sensitive: true
OpsiAbyssal:
  Scheduler:
    Sensitive: true
